package app.familygem.constant

import app.familygem.BuildConfig

/** Strings to convey the extras. */
object Extra {
    private const val PREFIX = BuildConfig.APPLICATION_ID // Package prefix

    const val ALONE = "$PREFIX.alone"
    const val AUTO_LOAD_TREE = "$PREFIX.autoLoadTree"
    const val DATE_ID = "$PREFIX.dateId"
    const val DESTINATION = "$PREFIX.destination"
    const val FAMILY_ID = "$PREFIX.familyId"
    const val FROM_FAMILY = "$PREFIX.fromFamilyActivity"
    const val ID = "$PREFIX.id"
    const val MEDIA_ID = "$PREFIX.mediaId"
    const val NOTE_ID = "$PREFIX.noteId"
    const val PAGE = "$PREFIX.page"
    const val PERSON_ID = "$PREFIX.personId"
    const val POSITION = "$PREFIX.position"
    const val RELATION = "$PREFIX.relation"
    const val RELATIVE_ID = "$PREFIX.relativeId"
    const val SOURCE_ID = "$PREFIX.sourceId"
    const val STRING = "$PREFIX.string"
    const val TEXT = "$PREFIX.text"
    const val TITLE = "$PREFIX.title"
    const val TREE_ID = "$PREFIX.treeId"
    const val TREE_ID_2 = "$PREFIX.treeId2"
    const val TYPE = "$PREFIX.type"
}
