<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/source"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:foreground="?android:attr/selectableItemBackground"
    android:minHeight="30dp"
    android:orientation="vertical"
    android:paddingStart="8dp"
    android:paddingTop="4dp"
    android:paddingEnd="8dp"
    android:paddingBottom="5dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/source_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toStartOf="@+id/source_image"
            android:ellipsize="end"
            android:maxLines="10"
            tools:text="Source title long enough to reach the image on the right and wrap to a second line.\nBut what about a third line?" />

        <ImageView
            android:id="@+id/source_image"
            android:layout_width="45dp"
            android:layout_height="60dp"
            android:layout_alignParentEnd="true"
            android:layout_marginStart="6dp"
            android:scaleType="centerCrop"
            android:visibility="gone"
            tools:src="@drawable/source_image" />

        <TextView
            android:id="@+id/source_citationsNum"
            style="@style/AppTheme.ReferencesNumber"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="-1dp"
            android:layout_marginEnd="-5dp"
            android:visibility="gone"
            tools:text="12" />
    </RelativeLayout>
</LinearLayout>
